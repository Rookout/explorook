type Repository {
  fullpath: String!
  repoName: String!
  id: String!
}

type FileInfo {
  path: String!
  name: String!
  isFolder: Boolean!
  size: Int
}

type Query {
  dir(repoId: String!, path: String!): [FileInfo]!
  file(repoId: String!, path: String!): String!
  search(repoId: String!, query: String!): [String]!
  listRepos: [Repository!]!
}