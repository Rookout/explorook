type Repository {
  fullpath: String!
  repoName: String!
  id: String!
}

type FileInfo {
  path: String!
  name: String!
  size: Int
  isFolder: Boolean!
}

type Query {
  dir(repoId: String!, path: String!): [FileInfo]!
  file(repoId: String!, path: String!): String!
  listRepos: [Repository!]!
}